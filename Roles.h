#ifndef ROLES_H
#define ROLES_H

// Genders
enum class Gender {
  MALE,
  FEMALE,
  ANY
};

// Parent Modifiers
enum class PMod {
  MATERNAL,
  PATERNAL,
  ANY
};

// Sibling Modifiers
enum class SMod {
  FULL,
  HALF,
  ANY
};

#endif
